#include "ListMenu.hpp"

namespace GUI {
	ListMenu::ListMenu(std::shared_ptr<sf::Font> font, sf::Vector2i position) :
		m_font(font),
		m_position(position),
		m_color(Color())
	{
	}
	void ListMenu::update(sf::RenderWindow & window, sf::Time & dt)
	{
	}
	void GUI::ListMenu::draw(sf::RenderWindow & window, sf::Time & dt) {
		
	}

	void ListMenu::updateDraw()
	{
	}

	
}
